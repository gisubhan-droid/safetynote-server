# SafetyNOTE — 전체 작업 계획서

> 최초 작성: 2026-06-19 (세션 37)
> 마지막 업데이트: 2026-06-21 (세션 54)

---

## ✅ 완료된 Phase 현황

| Phase | 내용 | 완료 세션 | 최종 커밋 |
|-------|------|-----------|-----------|
| Phase 1 | 핵심 기능 구현 + 파일럿 안정화 | 세션 1~34 | — |
| Phase 2 | Firebase FCM 외부 푸시 알림 | 세션 28~36 | `fcabd66` |
| Phase 5 | 브라우저 원클릭 서버 업데이트 자동화 | 세션 53 | `808959f` |
| 최적화 | 자동 DB 백업 + 페이지네이션 적용 | 세션 54 | `8f7d502` |

---

## 🔜 남은 Phase 진행 계획

### 권고 진행 순서

```
[즉시 가능] Phase 6 — 타 NAS 원클릭 설치 패키지
      ↓
[선택적]    Phase 3 — 코드 구조 정리 (기능 영향 없음)
      ↓
[마지막]    Phase 4 — NAS 설치 · 운영 매뉴얼 PDF
```

---

## 📦 Phase 6 — 타 NAS 원클릭 설치 패키지

> 목표: 신규 NAS에 SafetyNOTE를 명령어 한 줄로 설치

### 현재 상태
- `scripts/install.sh` v1.0 초안 작성 완료 (세션 51)
- 기본 흐름 구현됨 (clone → npm install → .env → PM2 시작)
- 실제 신규 NAS 환경 테스트 미완료

### 세부 작업 목록

| 단계 | 내용 | 예상 소요 |
|------|------|-----------|
| 6-1 | install.sh 완성 — 환경 검증 강화 (Node v18, PM2, git 확인) | 1시간 |
| 6-2 | .env 자동 생성 + 관리자 계정 초기화 로직 | 1시간 |
| 6-3 | HTTPS 인증서 자동 탐지 로직 연동 | 30분 |
| 6-4 | 원클릭 설치 테스트 + 설치 안내 README | 1시간 |

### 완료 기준
```bash
# 신규 NAS에서 이 한 줄로 전체 설치 완료
curl -fsSL https://raw.githubusercontent.com/gisubhan-droid/safetynote-server/main/scripts/install.sh | bash
```

---

## 🔧 Phase 3 — 코드 구조 정리 (선택적)

> 기능 변화 없음 — 개발 유지보수성 향상 목적

### 현재 상태
- `node-server.ts` 6,197줄 단일 파일
- 모든 라우트(87개)가 한 파일에 혼재
- 운영 중 기능 불편은 없음

### 세부 작업 목록

| 단계 | 내용 | 예상 소요 | 위험도 |
|------|------|-----------|--------|
| P3-1 | `src/db.ts` 생성 — rawDb 공유 모듈 | 30분 | ⭐☆☆☆☆ |
| P3-2 | 신규 라우트 파일 9개 생성 + 인라인 라우트 이동 | 2~3시간 | ⭐☆☆☆☆ |
| P3-3 | 기존 라우트 파일 7개에 인라인 라우트 병합 | 1~2시간 | ⭐⭐☆☆☆ |
| P3-4 | node-server.ts 정리 + 빌드 검증 + 커밋 | 1시간 | ⭐☆☆☆☆ |

### 분리 후 구조

```
node-server.ts (~200줄)    ← import + 서버 기동만 남음
src/
  db.ts                    ← rawDb 단일 export (신규)
  routes/
    push.ts                ← FCM 푸시 (신규)
    signature-requests.ts  ← 서명 요청 (신규)
    legal-notices.ts       ← 법적 공지 (신규)
    geocode.ts             ← GPS/주소 (신규)
    admin.ts               ← 관리자 설정 (신규)
    dist.ts                ← APK 배포 (신규)
    splice-reports.ts      ← 접속 보고서 (신규)
    unit-prices.ts         ← 단가 관리 (신규)
    events.ts              ← SSE 이벤트 (신규)
    + 기존 7개 파일 유지
```

---

## 📄 Phase 4 — NAS 설치 · 운영 매뉴얼 PDF

> Phase 6 완료 후 작성 — 비개발자 대상 문서

### 포함 내용 (예정)
- Synology NAS 초기 설정 (Node.js, PM2 설치)
- SafetyNOTE 원클릭 설치 방법
- 관리자 계정 초기화 및 첫 설정
- 일상 운영 방법 (백업 확인, 업데이트 방법)
- 장애 대응 FAQ

---

## 📊 장기 운영 최적화 로드맵

> 이미 적용된 항목과 향후 적용 예정 항목

| 항목 | 상태 | 적용 시점 |
|------|------|-----------|
| WAL + 32MB 캐시 + mmap PRAGMA | ✅ 완료 | 세션 35~ |
| 복합 인덱스 5개 (tasks/tbm/알림 등) | ✅ 완료 | 세션 35~ |
| 자동 DB 백업 (매일 새벽 2시, 30일 보관) | ✅ 완료 | 세션 54 |
| 오래된 알림 자동 정리 (90일 초과) | ✅ 완료 | 세션 54 |
| 페이지네이션 (tasks · splice_reports) | ✅ 완료 | 세션 54 |
| 토큰 서명 검증 강화 (HMAC) | ⏳ 중기 | 데이터 1,000건+ 시점 |
| VACUUM 자동화 (월 1회) | ⏳ 장기 | DB 500MB+ 시점 |
| 완료 공사 데이터 아카이브 | ⏳ 장기 | 운영 2년+ 시점 |

---

## 📌 결정 완료 사항 이력

| 날짜 | 결정 내용 |
|------|-----------|
| 2026-06-19 | 외선일보 워크시트 계획 **완전 취소** |
| 2026-06-19 | 배포 방식: 모든 수정 완료 후 **마지막 1회** NAS 배포 (현재는 수시 배포로 변경) |
| 2026-06-19 | TASK-001 삭제 처리: **연결 데이터 차단** 방식 (CASCADE 아님) |
| 2026-06-19 | TASK-004 탭 구성: 5개 탭 그룹 확정 (푸시/파일/GPS/APK/정보) |
| 2026-06-21 | Phase 5 완료 → 브라우저에서 직접 업데이트 가능 |
| 2026-06-21 | 최적화 우선 적용 (자동백업 + 페이지네이션) → Phase 6 다음 목표 |
